{
  "name": "christian-constant",
  "private": true,
  "version": "0.1.0",
  "description": "Conversational one-page site for Christian Constant — static page + Cloudflare Worker calling Claude",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && wrangler deploy",
    "dev": "node build.js && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
